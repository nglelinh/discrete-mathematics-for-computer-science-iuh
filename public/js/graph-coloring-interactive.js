(function () {
  function addStyle() { if (document.getElementById('gci-style')) return; var s = document.createElement('style'); s.id = 'gci-style'; s.textContent = '.gci-wrap{border:1px solid #e5e7eb;border-radius:12px;padding:16px;background:#fff;margin:14px 0}.gci-board{position:relative;height:280px;background:#f8fafc;border:1px solid #e5e7eb;border-radius:12px;margin:12px 0}.gci-edge{position:absolute;height:3px;background:#cbd5e1;transform-origin:left center}.gci-node{position:absolute;width:58px;height:58px;border-radius:42% 58% 45% 55%;border:3px solid #334155;background:#fff;display:flex;align-items:center;justify-content:center;font-weight:700;transform:translate(-50%,-50%);cursor:pointer;transition:.2s}.gci-node.sel{box-shadow:0 0 0 4px rgba(0,123,255,.25)}.gci-palette{display:flex;gap:10px;flex-wrap:wrap}.gci-swatch{width:42px;height:42px;border-radius:999px;border:2px solid #fff;box-shadow:0 0 0 1px #cbd5e1;cursor:pointer}.gci-msg{margin-top:10px;padding:10px;border-radius:8px;background:#e8f4fd;border-left:4px solid #007bff}.gci-msg.err{background:#ffebee;border-left-color:#e63946}'; document.head.appendChild(s); }
  function mount(host) { if (!host || host.dataset.gciMounted) return; host.dataset.gciMounted = '1'; addStyle(); var pts = [[50,16],[28,34],[72,34],[25,70],[50,83],[75,70]], edges = [[0,1],[0,2],[1,2],[1,3],[1,4],[2,4],[2,5],[3,4],[4,5]], colors = ['#e63946','#2a9d8f','#f4a261','#457b9d'], names = ['A','B','C','D','E','F'], picked = 0, fill = {}; host.innerHTML = '<div class="gci-wrap"><h4>🎨 Tô màu đồ thị</h4><p>Chọn vùng, rồi chọn màu. Hai vùng kề nhau không được cùng màu.</p><div class="gci-board"></div><div class="gci-palette"></div><div class="gci-msg"></div></div>'; var board = host.querySelector('.gci-board'), pal = host.querySelector('.gci-palette'), msg = host.querySelector('.gci-msg'); function line(a, b) { var p = pts[a], q = pts[b], dx = q[0] - p[0], dy = q[1] - p[1], len = Math.sqrt(dx * dx + dy * dy), d = document.createElement('div'); d.className = 'gci-edge'; d.style.left = p[0] + '%'; d.style.top = p[1] + '%'; d.style.width = len + '%'; d.style.transform = 'rotate(' + Math.atan2(dy, dx) + 'rad)'; board.appendChild(d); } function conflict(v, c) { return edges.some(function (e) { return (e[0] === v && fill[e[1]] === c) || (e[1] === v && fill[e[0]] === c); }); } function chromatic() { var used = {}; Object.keys(fill).forEach(function (k) { if (fill[k]) used[fill[k]] = true; }); return Object.keys(used).length; } function draw() { board.innerHTML = ''; edges.forEach(function (e) { line(e[0], e[1]); }); pts.forEach(function (p, i) { var n = document.createElement('button'); n.type = 'button'; n.className = 'gci-node ' + (i === picked ? 'sel' : ''); n.style.left = p[0] + '%'; n.style.top = p[1] + '%'; n.style.background = fill[i] || '#fff'; n.textContent = names[i]; n.addEventListener('click', function () { picked = i; draw(); }); board.appendChild(n); }); msg.className = 'gci-msg'; msg.textContent = 'Đang chọn vùng ' + names[picked] + '. Sắc số đang dùng: ' + chromatic() + '.'; } colors.forEach(function (c) { var b = document.createElement('button'); b.type = 'button'; b.className = 'gci-swatch'; b.style.background = c; b.addEventListener('click', function () { if (conflict(picked, c)) { msg.className = 'gci-msg err'; msg.textContent = 'Xung đột: vùng kề nhau không được cùng màu.'; return; } fill[picked] = c; draw(); }); pal.appendChild(b); }); draw(); }
  document.addEventListener('DOMContentLoaded', function () { document.querySelectorAll('[data-demo="graph-coloring-interactive"]').forEach(mount); });
}());
