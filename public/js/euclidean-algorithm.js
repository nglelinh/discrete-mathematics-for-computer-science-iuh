(function () {
  function addStyle() { if (document.getElementById('eua-style')) return; var s = document.createElement('style'); s.id = 'eua-style'; s.textContent = '.eua-wrap{border:1px solid #e5e7eb;border-radius:12px;padding:16px;background:#fff;margin:14px 0}.eua-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:12px;align-items:end}.eua-grid label{display:flex;flex-direction:column;gap:5px}.eua-grid input{padding:8px;border:1px solid #cbd5e1;border-radius:8px}.eua-out{margin-top:12px;padding:12px;border-radius:8px;background:#f8f9fa;line-height:1.7}.eua-step{padding:8px;border-left:4px solid #007bff;background:#e8f4fd;border-radius:8px;margin:6px 0}.eua-result{padding:10px;border-left:4px solid #28a745;background:#d4edda;border-radius:8px;margin-top:8px}'; document.head.appendChild(s); }
  function egcd(a, b) { if (b === 0) return {g: Math.abs(a), x: a < 0 ? -1 : 1, y: 0}; var r = egcd(b, a % b); return {g: r.g, x: r.y, y: r.x - Math.floor(a / b) * r.y}; }
  function mount(host) { if (!host || host.dataset.euaMounted) return; host.dataset.euaMounted = '1'; addStyle(); host.innerHTML = '<div class="eua-wrap"><h4>➗ Thuật toán Euclid</h4><div class="eua-grid"><label>a <input data-a type="number" value="48"></label><label>b <input data-b type="number" value="18"></label><button type="button" data-run>Tính GCD</button></div><div class="eua-out"></div></div>'; var out = host.querySelector('.eua-out'); function run() { var a0 = parseInt(host.querySelector('[data-a]').value, 10) || 0, b0 = parseInt(host.querySelector('[data-b]').value, 10) || 0, a = Math.abs(a0), b = Math.abs(b0), steps = []; if (!a && !b) { out.textContent = 'Không xác định khi a=b=0.'; return; } while (b !== 0) { var q = Math.floor(a / b), r = a % b; steps.push(a + ' = ' + q + '·' + b + ' + ' + r); a = b; b = r; } var ex = egcd(a0, b0); out.innerHTML = steps.map(function (x) { return '<div class="eua-step">' + x + '</div>'; }).join('') + '<div class="eua-result">GCD = <strong>' + a + '</strong></div><p>Euclid mở rộng: tìm x,y sao cho ax + by = gcd.</p><div class="eua-result">' + a0 + '·(' + ex.x + ') + ' + b0 + '·(' + ex.y + ') = ' + ex.g + '</div>'; } host.querySelector('[data-run]').addEventListener('click', run); run(); }
  document.addEventListener('DOMContentLoaded', function () { document.querySelectorAll('[data-demo="euclidean-algorithm"]').forEach(mount); });
}());
