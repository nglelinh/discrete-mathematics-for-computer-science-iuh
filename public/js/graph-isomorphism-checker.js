(function () {
  function addStyle() { if (document.getElementById('gic-style')) return; var s = document.createElement('style'); s.id = 'gic-style'; s.textContent = '.gic-wrap{border:1px solid #e5e7eb;border-radius:12px;padding:16px;background:#fff;margin:14px 0}.gic-panels{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:12px}.gic-board{position:relative;height:230px;background:#f8fafc;border:1px solid #e5e7eb;border-radius:12px}.gic-edge{position:absolute;height:3px;background:#94a3b8;transform-origin:left center}.gic-node{position:absolute;width:42px;height:42px;border-radius:999px;background:#fff;border:2px solid #007bff;color:#007bff;display:flex;align-items:center;justify-content:center;font-weight:700;transform:translate(-50%,-50%);z-index:2}.gic-out{margin-top:12px;padding:12px;border-radius:8px;background:#f8f9fa;line-height:1.7}.gic-map{padding:8px;border-left:4px solid #28a745;background:#d4edda;border-radius:8px}'; document.head.appendChild(s); }
  function mount(host) { if (!host || host.dataset.gicMounted) return; host.dataset.gicMounted = '1'; addStyle(); var pts = [[50,15],[85,45],[62,82],[20,66]], g1 = [[0,1],[1,2],[2,0],[2,3]], g2 = [[0,2],[2,1],[1,3],[3,0]], names1 = ['A','B','C','D'], names2 = ['1','2','3','4']; host.innerHTML = '<div class="gic-wrap"><h4>🔁 Kiểm tra đẳng cấu đồ thị</h4><div class="gic-panels"><div><strong>G1</strong><div class="gic-board" data-g="1"></div></div><div><strong>G2</strong><div class="gic-board" data-g="2"></div></div></div><button type="button" data-run>Kiểm tra đẳng cấu</button><div class="gic-out"></div></div>'; function draw(sel, edges, labels) { var b = host.querySelector(sel); b.innerHTML = ''; edges.forEach(function (e) { var p = pts[e[0]], q = pts[e[1]], dx = q[0] - p[0], dy = q[1] - p[1], line = document.createElement('div'); line.className = 'gic-edge'; line.style.left = p[0] + '%'; line.style.top = p[1] + '%'; line.style.width = Math.sqrt(dx * dx + dy * dy) + '%'; line.style.transform = 'rotate(' + Math.atan2(dy, dx) + 'rad)'; b.appendChild(line); }); pts.forEach(function (p, i) { var n = document.createElement('div'); n.className = 'gic-node'; n.style.left = p[0] + '%'; n.style.top = p[1] + '%'; n.textContent = labels[i]; b.appendChild(n); }); } function has(edges, a, b) { return edges.some(function (e) { return (e[0] === a && e[1] === b) || (e[0] === b && e[1] === a); }); } function perms(a) { if (a.length < 2) return [a]; var r = []; a.forEach(function (x, i) { perms(a.slice(0, i).concat(a.slice(i + 1))).forEach(function (p) { r.push([x].concat(p)); }); }); return r; } function check() { var d1 = [0,1,2,3].map(function (i) { return g1.filter(function (e) { return e[0] === i || e[1] === i; }).length; }).sort().join(','), d2 = [0,1,2,3].map(function (i) { return g2.filter(function (e) { return e[0] === i || e[1] === i; }).length; }).sort().join(','); if (d1 !== d2) { host.querySelector('.gic-out').innerHTML = 'Dãy bậc khác nhau: không đẳng cấu.'; return; } var found = null; perms([0,1,2,3]).some(function (p) { var ok = g1.every(function (e) { return has(g2, p[e[0]], p[e[1]]); }); if (ok) found = p; return ok; }); host.querySelector('.gic-out').innerHTML = found ? '<div class="gic-map">Có đẳng cấu: ' + found.map(function (v, i) { return names1[i] + '→' + names2[v]; }).join(', ') + '</div><p>Mọi cạnh của G1 được bảo toàn trong G2.</p>' : 'Không tìm được hoán vị bảo toàn cạnh.'; } draw('[data-g="1"]', g1, names1); draw('[data-g="2"]', g2, names2); host.querySelector('[data-run]').addEventListener('click', check); check(); }
  document.addEventListener('DOMContentLoaded', function () { document.querySelectorAll('[data-demo="graph-isomorphism-checker"]').forEach(mount); });
}());
