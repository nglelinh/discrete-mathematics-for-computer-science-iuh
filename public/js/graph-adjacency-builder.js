(function () {
  function addStyle() { if (document.getElementById('gab-style')) return; var s = document.createElement('style'); s.id = 'gab-style'; s.textContent = '.gab-wrap{border:1px solid #e5e7eb;border-radius:12px;padding:16px;background:#fff;margin:14px 0}.gab-board{position:relative;height:280px;background:#f8fafc;border:1px solid #e5e7eb;border-radius:12px;margin:12px 0}.gab-edge{position:absolute;height:4px;background:#cbd5e1;transform-origin:left center;border:0;cursor:pointer}.gab-edge.on{background:#007bff;box-shadow:0 0 0 3px rgba(0,123,255,.15)}.gab-node{position:absolute;width:44px;height:44px;border-radius:999px;background:#fff;border:2px solid #007bff;color:#007bff;display:flex;align-items:center;justify-content:center;font-weight:700;transform:translate(-50%,-50%);z-index:2}.gab-table{width:100%;border-collapse:collapse}.gab-table th,.gab-table td{border:1px solid #e5e7eb;padding:7px;text-align:center}.gab-deg{display:flex;gap:8px;flex-wrap:wrap;margin-top:10px}.gab-pill{padding:6px 10px;border-radius:999px;background:#e8f4fd;color:#0f172a}'; document.head.appendChild(s); }
  function mount(host) { if (!host || host.dataset.gabMounted) return; host.dataset.gabMounted = '1'; addStyle(); var pts = [[50,14],[84,38],[72,80],[28,80],[16,38]], edges = {}, names = ['A','B','C','D','E']; host.innerHTML = '<div class="gab-wrap"><h4>🕸️ Ma trận kề từ đồ thị</h4><p>Nhấn vào cạnh mờ để bật/tắt liên kết.</p><div class="gab-board"></div><table class="gab-table"></table><div class="gab-deg"></div></div>'; var board = host.querySelector('.gab-board'), table = host.querySelector('.gab-table'), deg = host.querySelector('.gab-deg'); function key(i, j) { return Math.min(i, j) + '-' + Math.max(i, j); } function drawBoard() { board.innerHTML = ''; for (var i = 0; i < 5; i++) for (var j = i + 1; j < 5; j++) { var x1 = pts[i][0], y1 = pts[i][1], x2 = pts[j][0], y2 = pts[j][1], dx = x2 - x1, dy = y2 - y1, len = Math.sqrt(dx * dx + dy * dy), e = document.createElement('button'); e.type = 'button'; e.className = 'gab-edge ' + (edges[key(i, j)] ? 'on' : ''); e.style.left = x1 + '%'; e.style.top = y1 + '%'; e.style.width = len + '%'; e.style.transform = 'rotate(' + Math.atan2(dy, dx) + 'rad)'; e.setAttribute('aria-label', 'Cạnh ' + names[i] + names[j]); (function (a, b) { e.addEventListener('click', function () { edges[key(a, b)] = !edges[key(a, b)]; paint(); }); }(i, j)); board.appendChild(e); } pts.forEach(function (p, i) { var n = document.createElement('div'); n.className = 'gab-node'; n.style.left = p[0] + '%'; n.style.top = p[1] + '%'; n.textContent = names[i]; board.appendChild(n); }); } function paint() { drawBoard(); var m = names.map(function (_, i) { return names.map(function (_, j) { return i !== j && edges[key(i, j)] ? 1 : 0; }); }); table.innerHTML = '<tr><th></th>' + names.map(function (x) { return '<th>' + x + '</th>'; }).join('') + '</tr>' + m.map(function (r, i) { return '<tr><th>' + names[i] + '</th>' + r.map(function (x) { return '<td>' + x + '</td>'; }).join('') + '</tr>'; }).join(''); deg.innerHTML = m.map(function (r, i) { return '<span class="gab-pill">deg(' + names[i] + ') = ' + r.reduce(function (a, b) { return a + b; }, 0) + '</span>'; }).join(''); } paint(); }
  document.addEventListener('DOMContentLoaded', function () { document.querySelectorAll('[data-demo="graph-adjacency-builder"]').forEach(mount); });
}());
