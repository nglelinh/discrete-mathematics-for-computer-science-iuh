(function () {
  function addStyle() { if (document.getElementById('mac-style')) return; var s = document.createElement('style'); s.id = 'mac-style'; s.textContent = '.mac-wrap{border:1px solid #e5e7eb;border-radius:12px;padding:16px;background:#fff;margin:14px 0}.mac-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:12px}.mac-grid label{display:flex;flex-direction:column;gap:5px}.mac-grid input{padding:8px;border:1px solid #cbd5e1;border-radius:8px}.mac-card{margin-top:12px;padding:12px;border-radius:10px;background:#f8fafc;border:1px solid #e5e7eb}.mac-result{padding:10px;border-left:4px solid #28a745;background:#d4edda;border-radius:8px;line-height:1.7}.mac-tables{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:12px}.mac-table{width:100%;border-collapse:collapse;background:#fff}.mac-table td,.mac-table th{border:1px solid #e5e7eb;padding:5px;text-align:center}.mac-table th{background:#e8f4fd}'; document.head.appendChild(s); }
  function mod(a, m) { return ((a % m) + m) % m; }
  function gcd(a, b) { a = Math.abs(a); b = Math.abs(b); while (b) { var t = a % b; a = b; b = t; } return a; }
  function inv(a, m) { var t = 0, nt = 1, r = m, nr = mod(a, m); while (nr) { var q = Math.floor(r / nr), x = t - q * nt; t = nt; nt = x; x = r - q * nr; r = nr; nr = x; } return r === 1 ? mod(t, m) : null; }
  function table(m, op) { var h = '<table class="mac-table"><tr><th>' + op + '</th>'; for (var i = 0; i < m; i++) h += '<th>' + i + '</th>'; h += '</tr>'; for (var r = 0; r < m; r++) { h += '<tr><th>' + r + '</th>'; for (var c = 0; c < m; c++) h += '<td>' + mod(op === '+' ? r + c : r * c, m) + '</td>'; h += '</tr>'; } return h + '</table>'; }
  function mount(host) { if (!host || host.dataset.macMounted) return; host.dataset.macMounted = '1'; addStyle(); host.innerHTML = '<div class="mac-wrap"><h4>🔢 Máy tính số học modulo</h4><div class="mac-grid"><label>a <input data-a type="number" value="7"></label><label>m <input data-m type="number" min="2" max="10" value="5"></label></div><div class="mac-card"><div class="mac-result"></div></div><div class="mac-card"><strong>Bảng phép toán trong Z<sub>m</sub></strong><div class="mac-tables"></div></div></div>'; function paint() { var a = parseInt(host.querySelector('[data-a]').value, 10) || 0, m = Math.max(2, Math.min(10, parseInt(host.querySelector('[data-m]').value, 10) || 5)); host.querySelector('[data-m]').value = m; var am = mod(a, m), addInv = mod(-a, m), mulInv = inv(a, m); host.querySelector('.mac-result').innerHTML = a + ' mod ' + m + ' = <strong>' + am + '</strong><br>Nghịch đảo cộng: ' + addInv + ' vì ' + am + ' + ' + addInv + ' ≡ 0 (mod ' + m + ')<br>Nghịch đảo nhân: ' + (mulInv === null ? 'không tồn tại vì gcd(' + a + ',' + m + ')=' + gcd(a, m) : '<strong>' + mulInv + '</strong> vì ' + am + '·' + mulInv + ' ≡ 1 (mod ' + m + ')'); host.querySelector('.mac-tables').innerHTML = '<div>' + table(m, '+') + '</div><div>' + table(m, '×') + '</div>'; } host.querySelectorAll('input').forEach(function (e) { e.addEventListener('input', paint); }); paint(); }
  document.addEventListener('DOMContentLoaded', function () { document.querySelectorAll('[data-demo="modular-arithmetic-calc"]').forEach(mount); });
}());
