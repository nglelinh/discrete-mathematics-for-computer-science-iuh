(function () {
  function addStyle() { if (document.getElementById('phv-style')) return; var s = document.createElement('style'); s.id = 'phv-style'; s.textContent = '.phv-wrap{border:1px solid #e5e7eb;border-radius:12px;padding:16px;background:#fff;margin:14px 0}.phv-controls{display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:12px;align-items:end}.phv-controls label{display:flex;flex-direction:column;gap:5px;color:#334155;font-size:14px}.phv-controls input{padding:8px;border:1px solid #cbd5e1;border-radius:8px}.phv-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(120px,1fr));gap:12px;margin:14px 0}.phv-box{min-height:110px;border:2px solid #e5e7eb;border-radius:12px;background:#f8fafc;padding:10px;transition:.2s}.phv-box.hot{border-color:#ffc107;background:#fff8dc;box-shadow:0 0 0 3px rgba(255,193,7,.22)}.phv-title{font-weight:700;color:#495057;margin-bottom:8px}.phv-item{display:inline-flex;align-items:center;justify-content:center;width:28px;height:28px;margin:3px;border-radius:999px;background:#007bff;color:#fff;font-size:13px}.phv-msg{padding:10px;border-radius:8px;background:#e8f4fd;border-left:4px solid #007bff}'; document.head.appendChild(s); }
  function mount(host) { if (!host || host.dataset.phvMounted) return; host.dataset.phvMounted = '1'; addStyle(); host.innerHTML = '<div class="phv-wrap"><h4>🐦 Trực quan Nguyên lý Dirichlet</h4><div class="phv-controls"><label>Số hộp n <input data-n type="number" min="1" max="12" value="3"></label><label>Số phần tử m <input data-m type="number" min="1" max="60" value="4"></label><button type="button" data-roll>Phân phối ngẫu nhiên</button></div><div class="phv-grid"></div><div class="phv-msg" aria-live="polite"></div></div>'; var nEl = host.querySelector('[data-n]'), mEl = host.querySelector('[data-m]'), grid = host.querySelector('.phv-grid'), msg = host.querySelector('.phv-msg'); function clamp(v, a, b) { v = parseInt(v, 10); return Number.isFinite(v) ? Math.max(a, Math.min(b, v)) : a; } function render() { var n = clamp(nEl.value, 1, 12), m = clamp(mEl.value, 1, 60), need = Math.ceil(m / n), boxes = Array.from({ length: n }, function () { return []; }); nEl.value = n; mEl.value = m; for (var i = 1; i <= m; i++) boxes[Math.floor(Math.random() * n)].push(i); grid.innerHTML = boxes.map(function (items, i) { return '<div class="phv-box ' + (items.length >= need ? 'hot' : '') + '"><div class="phv-title">Hộp ' + (i + 1) + ' (' + items.length + ')</div>' + items.map(function (x) { return '<span class="phv-item">' + x + '</span>'; }).join('') + '</div>'; }).join(''); msg.innerHTML = 'Ít nhất một hộp có &gt;= <strong>ceil(' + m + '/' + n + ') = ' + need + '</strong> phần tử.'; } host.querySelector('[data-roll]').addEventListener('click', render); nEl.addEventListener('input', render); mEl.addEventListener('input', render); render(); }
  document.addEventListener('DOMContentLoaded', function () { document.querySelectorAll('[data-demo="pigeonhole-visualizer"]').forEach(mount); });
}());
